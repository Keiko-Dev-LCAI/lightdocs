# Lightdocs (KeikoDev)

Phone-first Notes → Word on Lightchain AIVM.

**v1:** paste or photo notes → real `.docx` download (job id + polling).

- Frontend: this repo on GitHub Pages
- Backend: Railway (`server.py`)

Desktop brainstorm/demo folder is separate and never pushed.
